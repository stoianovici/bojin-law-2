# OPS-336: Update Remaining Pages to Use PageLayout

**Type**: UI Enhancement | **Complexity**: S | **Platform**: Desktop
**Status**: Closed
**Epic**: Linear UI Migration (OPS-331 to OPS-337)

## Problem

Only 5 of ~19 pages use the new `PageLayout` component. Remaining pages use inconsistent layout patterns.

## Approach

- Wrap each page with `PageLayout` component
- Use `PageHeader` for sticky headers with blur
- Apply `PageContent` for consistent max-width
- Ensure page transitions work correctly

## Done When

- [x] All main pages use PageLayout wrapper
- [x] Page headers are sticky with backdrop blur
- [x] Content areas have consistent max-width
- [x] Mobile breakpoints work correctly

## Pages Already Using PageLayout

- `apps/web/src/app/tasks/page.tsx` ✅
- `apps/web/src/app/cases/page.tsx` ✅
- `apps/web/src/app/documents/page.tsx` ✅
- `apps/web/src/app/time-tracking/page.tsx` ✅

## Pages Migrated

- `apps/web/src/app/analytics/page.tsx` ✅
- `apps/web/src/app/communications/page.tsx` ✅
- `apps/web/src/app/clients/[id]/page.tsx` ✅
- `apps/web/src/app/reviews/page.tsx` ✅
- `apps/web/src/app/settings/billing/page.tsx` ✅
- `apps/web/src/app/admin/ai-ops/page.tsx` ✅
- `apps/web/src/app/login/page.tsx` ✅
- `apps/web/src/app/page.tsx` (home/dashboard) ✅

## Session Log

### Session 1 (2025-12-28)

Migrated all 8 remaining pages to use the `PageLayout` component:

1. **analytics/page.tsx** - Wrapped `AnalyticsDashboard` with PageLayout
2. **communications/page.tsx** - Replaced `<main>` with PageLayout
3. **clients/[id]/page.tsx** - Added PageLayout + PageContent for loading, error, not found, and main states
4. **reviews/page.tsx** - Wrapped all states with PageLayout + PageContent
5. **settings/billing/page.tsx** - Wrapped SettingsContent and fallbacks with PageLayout
6. **admin/ai-ops/page.tsx** - Added PageLayout + PageContent for error and main states
7. **login/page.tsx** - Replaced div wrappers with PageLayout for all states
8. **page.tsx (home)** - Wrapped all role dashboards with PageLayout and added `withGlow` prop

All migrations verified with TypeScript compilation - no errors in migrated files.
