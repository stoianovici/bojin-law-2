# OPS-335: Migrate Task Management Components to Linear Design

**Type**: UI Enhancement | **Complexity**: M | **Platform**: Desktop
**Status**: Verifying
**Epic**: Linear UI Migration (OPS-331 to OPS-337)

## Problem

Task management is 73% migrated. KanbanBoard, ListView, and related components need Linear styling updates for consistency.

## Approach

- Update KanbanBoard columns to Linear card styling
- Migrate task cards to use consistent Linear patterns
- Apply Linear filter chips to TaskFilterBar
- Update calendar views with Linear colors
- Ensure drag-and-drop states use Linear feedback

## Done When

- [x] KanbanBoard uses Linear column styling
- [x] Task cards match Linear ListItem pattern
- [x] TaskFilterBar uses Linear FilterChips
- [x] Calendar views use Linear colors
- [x] All modals use Linear dialog styling

## Files

- `apps/web/src/components/task/KanbanBoard.tsx`
- `apps/web/src/components/task/ListView.tsx`
- `apps/web/src/components/task/TaskFilterBar.tsx`
- `apps/web/src/components/task/TaskDetailModal.tsx`
- `apps/web/src/components/task/TaskCard.tsx`
- `apps/web/src/components/task/CreateTaskModal.tsx`
- `apps/web/src/components/task/calendar/SpanningTaskCard.tsx`
- `apps/web/src/components/task/MultiWeekCalendarView.tsx`

## Session Log

### Session 1 (2025-12-28)

**Assessment**: Most task components were already using Linear design patterns (bg-linear-bg-secondary, border-linear-border-subtle, etc.). The main updates needed were:

1. **TaskFilterBar** - Migrated from Radix Checkbox to Linear FilterChips pattern
   - Uses `FilterChip`, `FilterChipsRow`, and `IconButton` from `@/components/linear/FilterChips`
   - More compact, pill-shaped filter chips for user selection
   - Cleaner active filter badges with rounded-full styling

2. **TaskDetailModal** - Updated to Linear dialog styling
   - Added animate-in animations (fade-in, slide-in-from-bottom-4)
   - Separated header/content/footer into distinct sections
   - Header and footer have border-linear-border-subtle/50 dividers
   - Footer has bg-linear-bg-tertiary background
   - Rounded-xl corners, smaller close button with hover states
   - Scrollable content area constrained to max-h-[calc(90vh-140px)]

**Already Compliant**:
- KanbanBoard - Already using Linear patterns
- ListView - Already using Linear patterns
- CreateTaskModal - Already using Linear patterns (matches TaskDetailModal now)
- MultiWeekCalendarView - Already using Linear patterns
- SpanningTaskCard - Already using Linear patterns
