# OPS-307: SectionHeading Component

**Type**: Feature | **Priority**: P1-High | **Platform**: Mobile
**Status**: Ready
**Created**: 2024-12-28

## Problem

Current `FeedSection` uses collapsible boxes with chevron icons. Need a simpler heading style that feels like a document section divider — small caps label with a horizontal rule.

## Approach

Create `SectionHeading.tsx`:

- Small caps title (uppercase, letter-spacing)
- Horizontal rule extending to the right
- Count badge at the end
- No collapse behavior (all sections visible)

```tsx
<div className="flex items-center gap-3 px-4 py-2 mt-4 first:mt-0">
  <span className="text-xs font-semibold text-gray-500 uppercase tracking-wide">{title}</span>
  <div className="flex-1 h-px bg-gray-200" />
  <span className="text-xs text-gray-400">({count})</span>
</div>
```

## Done When

- [x] `SectionHeading` renders with rule-line style
- [x] Props: `title`, `count`
- [x] First section has no top margin, subsequent sections have `mt-4`
- [x] Visual test at 390px width

## Files

- `apps/web/src/components/mobile/SectionHeading.tsx` - NEW

## Dependencies

None - can be developed in parallel with OPS-306.

## Session Log

### Session 1 - 2024-12-28

Created `SectionHeading.tsx` with:

- Small caps title (uppercase, letter-spacing: `tracking-wide`)
- Horizontal rule via `h-px bg-gray-200`
- Count badge in parentheses
- `withTopMargin` prop to control spacing (default true)
- TypeScript compiles clean

Ready for integration in OPS-308.
