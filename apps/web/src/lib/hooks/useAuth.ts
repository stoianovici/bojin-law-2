/**
 * useAuth Hook
 * Re-exports the useAuth hook from AuthContext for convenient importing
 * Story 2.4: Authentication with Azure AD
 */

export { useAuth } from '../../contexts/AuthContext';
export type { AuthContextType, AuthState } from '../../contexts/AuthContext';
