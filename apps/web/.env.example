# Web App (Next.js Frontend) - Environment Variables
# Copy this file to .env.local for local development

# ===== API Configuration =====
# GraphQL API Gateway endpoint (publicly accessible)
NEXT_PUBLIC_API_URL=http://localhost:4000/graphql

# Gateway URL for authentication (server-side API routes)
GATEWAY_URL=http://localhost:4000

# Gateway URL for client-side OAuth redirect
NEXT_PUBLIC_GATEWAY_URL=http://localhost:4000

# ===== Azure AD Authentication (Public) =====
# These are safe to expose to the browser
NEXT_PUBLIC_AZURE_AD_CLIENT_ID=your-client-id-here
NEXT_PUBLIC_AZURE_AD_TENANT_ID=your-tenant-id-here
NEXT_PUBLIC_AZURE_AD_REDIRECT_URI=http://localhost:3000/api/auth/callback

# ===== Application Configuration =====
NODE_ENV=development
PORT=3000

# ===== Optional: Analytics =====
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ===== HTTP Basic Auth (Production/Staging Protection) =====
# Leave empty to disable authentication (for local development)
# Set these in Render/production to password-protect the site
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=
